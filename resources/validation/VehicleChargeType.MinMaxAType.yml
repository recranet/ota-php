Recranet\OTA\VehicleChargeType\MinMaxAType:
    properties:
        maxChargeDays:
            -
                Regex:
                    pattern: '~[\-+]?[0-9]+~u'
                    groups:
                        - xsd_rules
